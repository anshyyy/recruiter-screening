/** Product name shown in the app shell and browser metadata. */
export const APP_DISPLAY_NAME = 'Recruiter Screening';

/** Short mark for tight mobile app bars (paired with aria-label on the control). */
export const APP_SHORT_MARK = 'RS';
